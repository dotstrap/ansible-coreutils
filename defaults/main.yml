---
# defaults file for dotstrap-coreutils

dotstrap_config_dir: "{{ ansible_user_dir }}/.config/dotstrap"

coreutils_marker: "#####=== {mark} Ansible managed GNU coreutils settings ===#####"

install_state: present
configuration_state: present

coreutils_name: coreutils
